import 'package:flutter/material.dart';

class MobileScreenLay extends StatelessWidget {
  const MobileScreenLay({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('A mobile')),
    );
  }
}