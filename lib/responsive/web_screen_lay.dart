import 'package:flutter/material.dart';

class WebScreenLay extends StatelessWidget {
  const WebScreenLay({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('A Web')),
    );
  }
}